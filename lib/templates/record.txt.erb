---
Order: <%= number %>
Delivery: <%= delivery.name %>
Total: £<%= total.to_f %>

Basket:
<% basket.each do |name, hash| %>
  <%= hash[:product].name %> (x<%= hash[:quantity] %>)
<% end %>

Customer:
Name: <%= customer['name'] %>,
Email: <%= customer['email'] %>

Address:
  <%= address['address1'] %>,<% unless address['address2'] == '' %>
  <%= address['address2'] %>,<% end %>
  <%= address['city'] %>,
  <%= address['county'] %>,
  <%= address['country'].name %>,
  <%= address['postcode'] %>
---
